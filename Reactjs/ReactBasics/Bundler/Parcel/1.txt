✅ List of what Parcel does:

- Bundles JavaScript, CSS, HTML, images, and more
- Supports zero config setup
- Automatically installs missing dependencies
- Hot Module Replacement (HMR)
- Tree shaking
- Code splitting
- Minification
- Source maps generation
- Caching for faster builds
- Transpiles modern JS (via Babel internally)
- Supports TypeScript out of the box
- Supports JSX and React
- Handles static assets (images, fonts, etc.)
- PostCSS support
- Environment variable support
- Automatic polyfilling
- Parallel builds using workers
- Built-in development server
- Supports multiple targets (browser, node)
- Generates production-ready optimized output
- Plugin system for customization
- Handles async imports
- Resolves node_modules
- Can output as ESModules or CommonJS


🛠️ Parcel-Specific Features
Parcel also does some of these automatically without needing configuration:
Automatic plugin detection (e.g., if you install sass, it just works).
Built-in support for many file types (e.g., JSON, WebAssembly, WASM).
Blazing-fast caching and parallel builds.
Zero-config TypeScript support.
Smart default settings.

