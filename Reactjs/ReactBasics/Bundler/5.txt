✅ What is a Plugin (General Definition)
A plugin is a tool or piece of software that adds extra features or functionality to an existing program, 
without changing its core code.

🧠 Example: Like a Chrome extension — adds power without touching the browser itself.
---
🔧 What is a Plugin in Webpack?
A Webpack plugin is a JavaScript object that helps Webpack do extra tasks during the build process, 
such as:
- Generating HTML files
- Cleaning folders
- Copying assets
- Injecting variables
- Optimizing code
---

🛠 Example Plugin Usage in Webpack
plugins: [
  new HtmlWebpackPlugin({ template: './index.html' })
]

✅ What it does:
- Generates an HTML file in the `dist/` folder
- Automatically adds <script> tags for bundled JS
- Saves you from doing it manually

---

🤔 Why Aren’t Plugins Included by Default?
Because not every project needs every feature.
Webpack is made to be:
- Flexible ✅  
- Lightweight ✅  
- Customizable ✅  
So, you only install the plugins you actually need.

---

📦 Common Webpack Plugins

| Plugin Name             | Purpose                                                      |
|------------------------|--------------------------------------------------------------|
| HtmlWebpackPlugin       | Auto-generates HTML with injected <script> tag               |
| CleanWebpackPlugin      | Deletes old files in `dist/` folder before new build         |
| MiniCssExtractPlugin    | Extracts CSS into a separate file (not inside JS)            |
| DefinePlugin            | Injects global constants like `process.env.NODE_ENV`         |
| CopyWebpackPlugin       | Copies static files like images or fonts to output folder    |

---

