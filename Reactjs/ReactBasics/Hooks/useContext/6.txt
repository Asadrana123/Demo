🔹 2. Component calls useContext(ThemeContext)

React does the following:
Registers this component (its fiber) as a subscriber to ThemeContext
Stores a dependency inside the component’s fiber:

fiber.dependencies = {
  lanes: 0,
  firstContext: {
    context: ThemeContext,
    memoizedValue: 'light',
    next: null
  }
}
