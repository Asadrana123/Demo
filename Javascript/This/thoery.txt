this:-this refers to the current context or object from which a function is being called — essentially, 
the object that is executing the current piece of code.

:-If this is null or undefined, JavaScript will default this to the 
global object (window in browser, global in Node.js).

uses:-
1.OOP/methods
2.Constructor functions
3.Event Listeners in DOM:- document.getElementById('btn').addEventListener('click', function () {
  this.style.backgroundColor = 'blue'; // Refers to the button
});
4.