
Lexical context → the environment where a piece of code currently exists and resolves variables.
Lexical context (lexical environment) in JavaScript is created only by:
Functions
Blocks ({} in if, for, while, etc.)
Modules

A module lexical context = the scope created around an entire ES module file.