The Event Loop is the mechanism in JavaScript that handles asynchronous operations.
The Event Loop constantly checks: if the call stack is empty and there are pending tasks, it moves
them into the call stack to be executed.

