different between let and const
1. Reassignment:
let: Can be reassigned.

const: Cannot be reassigned.

2. Immutability:
let: Value can be changed (even for objects/arrays).

const: Reference is immutable; the value can still be mutated (for objects/arrays).

